import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.chainbreaker.app',
  appName: 'ChainBreaker',
  webDir: 'dist',
  android: { backgroundColor: '#101312' }
};

export default config;
